Branch feature/auto_update_on_cat_registration will be used to allow the system to automatically update
all of the unknown kitty visits every time a new kitty is registered.  In the last test we had to
manually update each visit which was very tedious and should have been automated.

21 MARCH 2026

1. Kitty detection will be via polling loop using scale and/or camera.
    a. Probably scale initially.
2. Data buffer will contain some previous data so we can get the pre-weight in addition
to the current weight.
3. Entry event on significant increase in weight.  Exit event on significant drop.
    a. Should set thresholds.
4. Use BOTH the kitty weight AND the camera image to reason about identity.
5. Decide on how to call the agent on event.
    a. Python function call? (let's use this!)
    b. CLI method (current method!)
    c. REST API?
    d. Other?
6. Based on Python function call, we should create a Python API that other applications use.
    a. And we need a setup.py and need to make this into a package that the developer can 
    install and then call.
    b. And then write the test scripts in Python for demonstration purposes.
7. Now regarding chip reading, even if you can't decrypt the chip, as long as the chip 
reader output is unique for each kitty and repeatable even if you cannot decrypt, you still
have a unique identifier!
    a. So add chip ID string capability!
    b. This would be gold standard.

4 APRIL 2026

1. Successful litterbox simulation using actual kitty images.
    a. We need to plan on real world testing at this stage.
    b. Al recommends starting with weight only for simplicity.
        (1) I must confirm this with Al today to be sure I am remembering it 
        correctly because I could be misremembering things here!
2. Need plans going forward.
    a. Present system can accomodate weight sensing, image sensing, ammonia, and 
    methane.
    b. Should adjust the present system to add chip ID.
        (1) Even if scrambled as long as it's unique to each cat it's good enough.
        (2) Should be taken as definitive.
            (a) But what if two cats are near the box at the same time?
            Maybe not so definitive?  I dont' know...
3. Question: When does hardware testing begin?
4. Ryan will merge present system to main branch in his repo.
5. Next week (tentative):
    a. Pour through the data.
    b. Work out how to get an initial operating run with hardware going.


CODE DIRECTIONS:

I. Take time domain measurements:
    A. Weight 
    B. Image similarity to known cats.
    C. Other sensors.
    D. Save time domain measurements.
    E. Save summaries of time domain measures as min/max/median/mean 
II. Analyze results. Inspired heavily by IEEE Aero 2020 paper.
    A. Short term analysis:
        1. Eigenvalue/eigenvector.
        2. Dominant subspace vs noise subspace.
        3. Explained variance.
            a. Important: long term changes in the vector subspace for each kitty
        4. Expansion coefficeints.
            a. Move beyond the simple vector Gaussian.
            b. Use automatec clustering algorithms.
            c. Analyze clusters and look for drift.
            d. Important: long term application of cluster drift.
    B. Long term analysis 
        1. Using statistics above, trend min/max/mean/median.
        2. Generate the percentile bands.
        3. Trend min/max/mean/median and percentiles.
        4. Look for significant deviations from trend.
    C. Save waveforms.
        1. Better analysis than the admittedly crude algorithsm of the 
        IEEE Aero 2020 paper can be applied later.
III. KEY STEPS
    A. Waveform gathering.
        1. For each box visit gather time domain waveforms of:
            a. Image similarity to known kitties.
            b. Weight measurements.
            c. Other sensors.
            d. Images (but limit to one week or to one month to avoid memory overload)
    B. Short term analysis 
        1. Using eigenvector/eigenvalue for initial system and store the 
        expansions for each kitty and each sensor type.
        2. Store clusters of expansion coefficients for cluster analysis and please 
        move beyond simple vector Gaussian.
    C. Long term analysis 
        1. Trending of min/max/mean/median/percentiles 
        2. Catch anomalous points w.r.t. trend.
        3. Catch anomalous trends.
    D. Note that the waveforms can be used for future better algorithms.
    E. Note that images not just of the box but of the kitties can be used 
    for AI based health detection.
        1. For example upload perhaps two kitty images for each kitty per 
        week to cloud for health check and warning.
        2. And also upload the suspected anomalous litter box images.

